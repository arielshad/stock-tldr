# DxbEstate Intel — SEO Status & Strategy

Single source of truth for SEO work on this repo. Update this file whenever
you ship, remove, or plan an SEO change — future-you should be able to read
it top-to-bottom and know exactly what the site looks like to a crawler.

**Last updated**: 2026-05-05
**Public domain**: https://dxb-estate-intel.xyz
**Canonical host**: `dxb-estate-intel.xyz` (Cloudflare Pages / Workers)

## 1. Crawlable pages

| Path                    | Generator                  | Target keyword(s)                              | Schema                          |
| ----------------------- | -------------------------- | ---------------------------------------------- | ------------------------------- |
| `/`                     | `scripts/prerender.ts`     | _Dubai property intelligence_, _Dubai listings_       | `WebSite` + `Organization`      |
| `/sources`          | `scripts/prerender.ts`     | _Dubai property sources_, _DLD RERA portals_    | `CollectionPage`                |
| `/log`                  | `scripts/prerender.ts`     | _Dubai property changelog_, _what moved Dubai property_      | `CollectionPage`                |
| `/opportunities/<id>` (×N)   | `scripts/prerender.ts`     | item-specific (area/building + source + opportunity)        | `Article` + `BreadcrumbList`    |
| `/sitemap.xml`          | `scripts/prerender.ts`     | —                                              | —                               |
| `/robots.txt`           | `scripts/prerender.ts`     | —                                              | —                               |

Cloudflare serves static files first and falls back to `/index.html` for
anything else (see `public/_redirects`), so the SPA router handles unknown
paths without 404-ing.

## 2. Meta tag strategy

- **Titles**: primary keyword first, pipe separator, brand suffix. Hard
  cap ≤ 60 chars so Google doesn't truncate the brand.
- **Descriptions**: 120–158 chars, natural keyword usage, include a
  concrete value prop ("refreshed every 2 hours", "every market-moving
  opportunity"). Never keyword-stuffed.
- **OG/Twitter**: auto-generated by `renderMetaBlock` in
  [scripts/prerender.ts](scripts/prerender.ts) from the same `PageMeta`
  object as the regular `<title>` + `<meta description>`. One source of
  truth per page.
- **Canonical**: always absolute, always `https://`, never trailing slash
  except `/`. Env-overridable via `SITE_URL`.
- **OG image**: per-opportunity items use `item.image.url` (the verified
  opportunity hero); home / `/sources` / `/log` fall back to
  `${SITE_URL}/og-image.png` (ships in `public/`).

### Current page meta

**Home** — `Dubai Real-Estate Intelligence — Listings, Leads, Deals | DxbEstate Intel`
> Dubai property opportunities, duplicate listings, seller leads, developer
> launches, DLD/rent signals and deal checks — refreshed every 2 hours.

**Sources** — `Dubai Property Source Map — Portals, DLD, Developers | DxbEstate Intel`
> A source map of Dubai portals, DLD/RERA data, developers, auctions,
> brokerages and market-data feeds used by the collection workflow.

**Log** — `Collection Log — Dubai Listing Changes & Deal Signals | DxbEstate Intel`
> The full changelog of DxbEstate Intel sweeps — every opportunity added,
> updated or removed, with coverage and a one-line rationale.

**Opportunity pages** — built by `buildOpportunityTitle()` in prerender:
1. `${title} — ${org} | DxbEstate Intel` (preferred)
2. `${title} | DxbEstate Intel` (drop org if #1 > 60ch)
3. `${title}` alone (if title itself already ≥ 60ch)

## 3. Schema / JSON-LD strategy

Each page ships exactly ONE JSON-LD block matching its content type. The
dev-mode fallback block in [index.html](index.html) is stripped during
prerender and replaced with the page-specific block before `</head>`.

| Page             | JSON-LD                                                             |
| ---------------- | ------------------------------------------------------------------- |
| `/`              | `@graph` of `WebSite` (`@id:/#website`) + `Organization` (`@id:/#org`, with logo `ImageObject`) |
| `/sources`   | `CollectionPage` with `isPartOf` → `/#website`, `publisher` → `/#org` |
| `/log`           | `CollectionPage` (same refs as sources)                         |
| `/opportunities/<id>` | `Article` (headline, description, datePublished, dateModified, author, publisher ref, image ImageObject, mainEntityOfPage, articleSection, keywords) **+** `BreadcrumbList` (Home → opportunity title) |

All opportunity Article schemas carry `dateModified` from `feed.generatedAt`
so crawlers get a signal every time the 2h cron refreshes the feed.

## 4. H1 hierarchy

Google expects exactly one `<h1>` per page describing the page topic. The
brand renders as `<h1>` **only on the feed home**, where "DxbEstate Intel" IS
the page topic. On `/sources` the brand is a `<p>` and
`<h1 class="inf-title">Top Dubai property Voices to Follow</h1>` is the visible
page title. Same pattern on `/log` with
`<h1 class="log-title">Dubai property Changelog</h1>`.

CSS uses `text-transform: uppercase` so the visual stays brutalist while
the source text is sentence-case (what Google actually indexes).

## 5. Sitemap

Regenerated on every `bun run build` by `scripts/prerender.ts`. Includes:

- `/` — priority 1.0, daily
- `/sources` — priority 0.8, weekly
- `/log` — priority 0.7, daily
- `/opportunities/<id>` — priority 0.6, monthly, `lastmod = item.date`

## 6. Robots

[dist/robots.txt](dist/robots.txt) allows all crawlers on everything and
points at `${SITE_URL}/sitemap.xml`. No `Disallow` entries.

`<meta name="robots">` in the HTML shell is
`index, follow, max-image-preview:large, max-snippet:-1` — lets Google
show the opportunity hero image in rich results and use full-length snippets
for the explainer copy.

## 7. Open / planned

Ordered by impact.

### High impact

- [ ] **Add a real RSS/Atom feed at `/feed.xml`** — high ROI for a Dubai property
  tracker. Should emit the last N items from `opportunities.json` with full
  tagline + image + area/building codes. Prerender step, no UI change.
- [ ] **Submit sitemap to Google Search Console + Bing Webmaster Tools**
  once `dxb-estate-intel.xyz` is live. Verify via DNS or HTML file. Bing also
  accepts the sitemap via IndexNow.
- [ ] **Claim the brand on X/Twitter and add `<meta name="twitter:site"
  content="@dxbestateintel">`** once the handle exists.
- [ ] **Per-area tag pages**: `/areas/DXB-MARINA`, `/areas/JVC` etc.
  Area-specific landing pages can capture "Dubai Marina deals" and "JVC yield" searches. Generate one page per distinct area/building code observed in `opportunities.json`. Filter view of the feed scoped to that code.

### Medium impact

- [ ] **`SearchAction` in WebSite JSON-LD** — requires honoring
  `?q=<query>` as an initial search state in `App.tsx` `parseRoute()`.
  Unlocks Google sitelinks search box.
- [ ] **Add `FinancialProduct` / `Corporation` JSON-LD on opportunity pages
  with verified area/building codes** — links the opportunity to a stock entity.
- [ ] **Add `mentions` / `about` to Article schema** with `Corporation`
  entities for each area/building — improves entity-level SEO.
- [ ] **Internal linking from opportunity modal → related opportunities** (same
  primary area/building code, same org, or same category). Right now each opportunity
  page is a dead-end other than the brand link.

### Lower impact / nice to have

- [ ] **hreflang `x-default`** — single-language site but prevents warnings.
- [ ] **Preconnect to `bloomberg.com`, `reuters.com`** (top sources for
  opportunity-image OGs) — shaves ~100ms off LCP.
- [ ] **Skip-link for accessibility** (also a ranking signal).
- [ ] **`<link rel="preload" as="font">`** for JetBrains Mono + Inter.
- [ ] **`article:author` OG tag** on opportunity pages (currently emit
  `article:published_time` only).

## 8. Maintenance playbook

**Whenever a new PAGE TYPE ships** (new route, new content surface):

1. Add a new `PageMeta` constant in `scripts/prerender.ts`.
2. Add a matching JSON-LD helper (or reuse `renderJsonLdCollectionPage`).
3. Wire it into `main()` with `injectMeta(template, META, jsonLd)`.
4. Add the page to the `buildSitemap()` call.
5. Add the route to `parseRoute()` in `src/App.tsx` and update the
   `document.title` effect.
6. Add a semantic `<h1>` to the component, and make sure the brand is
   NOT an `<h1>` when that page is active.
7. Update sections 1–3 of this file.
8. `bun run build` and verify the new `dist/<path>/index.html` has the
   right `<title>`, meta description, canonical, JSON-LD type.

**Whenever the AGENT runs a sweep** (every 2h via cron):

- `opportunities.json` gets rewritten. No SEO action needed — prerender
  regenerates all opportunity pages + sitemap on the next build.
- `sweeps.json` gets one new entry. `/log` regenerates automatically.

**Whenever the PUBLIC DOMAIN changes**:

- Update `SITE_URL` default in [scripts/prerender.ts](scripts/prerender.ts).
- Update absolute URLs hardcoded in [index.html](index.html) JSON-LD fallback.
- Update references in [CLAUDE.md](CLAUDE.md) and this file.
- Set up a 301 redirect from the old host (Cloudflare rule).
- Submit the new sitemap to Search Console, retire the old property.
- Grep for stale URLs: `grep -rn "old-host" . --include="*.{ts,tsx,json,md,html,yml}"`.

## 9. Verification commands

Quick sanity checks after any SEO change:

```bash
# Full build (tsc + vite + prerender)
bun run build

# Titles + descriptions on all index pages
grep -E "<title>|description" dist/index.html dist/sources/index.html dist/log/index.html

# JSON-LD presence + type on each page type
grep '"@type"' dist/index.html
grep '"@type"' dist/sources/index.html
grep '"@type"' dist/log/index.html

# Sitemap sanity
grep -c "<url>" dist/sitemap.xml
head -20 dist/sitemap.xml

# No stale domain references
grep -rn "ai-tldr.dev\|workers.dev" dist/ 2>/dev/null | head
```

**External validators** (use after deploy):

- [Google Rich Results Test](https://search.google.com/test/rich-results)
- [Schema Markup Validator](https://validator.schema.org/)
- [Twitter Card Validator](https://cards-dev.twitter.com/validator) (cached)
- [Facebook Sharing Debugger](https://developers.facebook.com/tools/debug/)
- [Google PageSpeed / Lighthouse](https://pagespeed.web.dev/)
